import LoanStatusBadge from './LoanStatusBadge';
const money=(n)=>`KES ${Number(n||0).toLocaleString()}`;
export default function LoanPortfolio({ portfolio, onDecision, onDisburse, busy }) { if (!portfolio) return null; const metrics=[['Outstanding',portfolio.summary.total_outstanding],['Disbursed',portfolio.summary.total_disbursed],['Overdue',portfolio.summary.overdue],['Defaulted',portfolio.summary.defaulted],['Interest earned',portfolio.summary.interest_earned],['Awaiting decision',portfolio.summary.awaiting_decision_count]]; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div><h2 className="font-bold">Loan portfolio</h2><p className="text-sm text-slate-500">Official view: exposure, approval queue and disbursement readiness.</p></div><div className="mt-4 grid gap-3 sm:grid-cols-3 lg:grid-cols-6">{metrics.map(([label,value])=><div key={label} className="rounded-xl bg-slate-50 p-3 dark:bg-slate-800"><p className="text-xs text-slate-500">{label}</p><b className="text-sm">{label==='Awaiting decision'?value:money(value)}</b></div>)}</div><div className="mt-4 space-y-2">{portfolio.loans.map(loan=><div key={loan.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-slate-100 p-3 dark:border-slate-800"><div><p className="font-semibold">{loan.member_name} <span className="font-normal text-slate-500">· {loan.reference}</span></p><p className="text-sm text-slate-500">Outstanding: {money(loan.outstanding)} · {loan.days_late} days late</p></div><div className="flex items-center gap-2"><LoanStatusBadge status={loan.status}/>{loan.status==='pending_approval'&&<><button disabled={busy} onClick={()=>onDecision(loan.id,'rejected')} className="rounded-lg border px-3 py-1.5 text-sm">Reject</button><button disabled={busy} onClick={()=>onDecision(loan.id,'approved')} className="rounded-lg bg-emerald-600 px-3 py-1.5 text-sm font-semibold text-white">Approve</button></>}{loan.status==='approved'&&<button disabled={busy} onClick={()=>onDisburse(loan.id)} className="rounded-lg bg-sky-600 px-3 py-1.5 text-sm font-semibold text-white">Disburse</button>}</div></div>)}</div></section>; }
