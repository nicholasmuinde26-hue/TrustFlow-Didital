import { useState } from 'react';
const initial = { amount: '', purpose: '', repayment_period_months: '3', repayment_frequency: 'monthly', disbursement_method: 'mpesa', phone_number: '' };
export default function LoanApplicationForm({ canApply, onSubmit, busy }) { const [form, setForm] = useState(initial); const change=(key)=>(e)=>setForm({...form,[key]:e.target.value}); const submit=(e)=>{e.preventDefault(); onSubmit({...form,amount:Number(form.amount),repayment_period_months:Number(form.repayment_period_months)}).then?.(()=>setForm(initial));}; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div><p className="text-sm font-bold">New loan application</p><p className="mt-1 text-sm text-slate-500">We check your policy, savings and current exposure before submitting.</p></div><form onSubmit={submit} className="mt-5 space-y-3"><label className="block text-sm font-medium">Amount (KES)<input required min="1" type="number" value={form.amount} onChange={change('amount')} className="mt-1 w-full rounded-xl border bg-transparent p-2.5" placeholder="30,000" /></label><label className="block text-sm font-medium">Purpose<input required value={form.purpose} onChange={change('purpose')} className="mt-1 w-full rounded-xl border bg-transparent p-2.5" placeholder="School fees" /></label><div className="grid grid-cols-2 gap-3"><label className="text-sm font-medium">Term<select value={form.repayment_period_months} onChange={change('repayment_period_months')} className="mt-1 w-full rounded-xl border bg-transparent p-2.5">{[1,2,3,6,12].map(n=><option key={n} value={n}>{n} month{n>1?'s':''}</option>)}</select></label><label className="text-sm font-medium">Frequency<select value={form.repayment_frequency} onChange={change('repayment_frequency')} className="mt-1 w-full rounded-xl border bg-transparent p-2.5"><option value="monthly">Monthly</option><option value="weekly">Weekly</option></select></label></div><label className="block text-sm font-medium">M-Pesa phone<input value={form.phone_number} onChange={change('phone_number')} className="mt-1 w-full rounded-xl border bg-transparent p-2.5" placeholder="0712 345 678" /></label><button disabled={!canApply || busy} className="w-full rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white shadow-sm transition hover:bg-emerald-700 disabled:cursor-not-allowed disabled:opacity-50">{busy ? 'Checking eligibility…' : canApply ? 'Check eligibility & submit' : 'You cannot apply right now'}</button></form></section>; }
