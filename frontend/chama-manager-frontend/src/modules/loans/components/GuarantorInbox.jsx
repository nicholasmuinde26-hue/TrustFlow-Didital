const money=(n)=>`KES ${Number(n||0).toLocaleString()}`;
export default function GuarantorInbox({ requests, onRespond, busy }) { if (!requests?.length) return null; return <section className="rounded-2xl border border-amber-200 bg-amber-50 p-5 dark:border-amber-900 dark:bg-amber-950/30"><p className="text-sm font-bold text-amber-900 dark:text-amber-200">Guarantor inbox</p><p className="mt-1 text-sm text-amber-800 dark:text-amber-300">Accepting creates an exposure against your available guarantee capacity.</p><div className="mt-4 space-y-3">{requests.map(r=><div key={r.loan_id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl bg-white p-3 dark:bg-slate-900"><div><p className="font-semibold">{r.reference || 'Loan request'} · {money(r.guaranteed_amount)}</p><p className="text-sm text-slate-500">Purpose: {r.purpose}</p></div><div className="flex gap-2"><button disabled={busy} onClick={()=>onRespond(r.loan_id,'declined')} className="rounded-lg border px-3 py-2 text-sm font-semibold">Decline</button><button disabled={busy} onClick={()=>onRespond(r.loan_id,'accepted')} className="rounded-lg bg-emerald-600 px-3 py-2 text-sm font-semibold text-white">Accept</button></div></div>)}</div></section>; }
