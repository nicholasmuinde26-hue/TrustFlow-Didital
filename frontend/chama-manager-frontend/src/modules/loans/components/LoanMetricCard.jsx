const accents = { emerald: 'border-t-emerald-500', sky: 'border-t-sky-500', amber: 'border-t-amber-500' };
export default function LoanMetricCard({ label, value, hint, accent = 'emerald' }) { return <div className={`rounded-2xl border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-800 dark:bg-slate-900 border-t-4 ${accents[accent] || accents.emerald}`}><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold tracking-tight">{value}</p>{hint && <p className="mt-1 text-xs text-slate-500">{hint}</p>}</div>; }
