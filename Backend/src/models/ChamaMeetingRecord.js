import mongoose from "mongoose";
const schema = new mongoose.Schema({ chama_id: { type: mongoose.Schema.Types.ObjectId, ref: "Chama", required: true, index: true }, title: { type: String, required: true }, agenda: [{ text: String, completed: { type: Boolean, default: false } }], attendance: [{ membership_id: { type: mongoose.Schema.Types.ObjectId, ref: "ChamaMembership" }, checked_in_at: Date, check_in_code: String }], votes: [{ question: String, options: [String], ballots: [{ membership_id: { type: mongoose.Schema.Types.ObjectId, ref: "ChamaMembership" }, option: String }], resolution: String }], minutes: { type: String, default: "" }, status: { type: String, enum: ["scheduled", "live", "closed"], default: "scheduled" }, created_by: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true } }, { timestamps: true });
export default mongoose.models.ChamaMeetingRecord || mongoose.model("ChamaMeetingRecord", schema);
